# Keep pnpm's isolated linker so native-package lifecycle shims resolve from
# node_modules/.pnpm on Windows. Platform bindings needed by tools are pinned
# explicitly in package.json instead of relying on hoisting.
node-linker=isolated
