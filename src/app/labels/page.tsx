import LabelPackReviewer from "@/components/LabelPackReviewer";

export const metadata = {
    title: "Label pack review · SignRepair",
};

export default function LabelsPage() {
    return <LabelPackReviewer />;
}
